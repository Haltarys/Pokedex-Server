import { MongoUniqueFieldFilter } from './mongo-unique-field.filter';

describe('MongoUniqueFieldFilter', () => {
  it('should be defined', () => {
    expect(new MongoUniqueFieldFilter()).toBeDefined();
  });
});
